
<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <%- include('../partials/header.ejs') %>



<main>
    <div class="container">
    <article class="card">
        <div class="card-left">
            <figure>
                <img src="/images/users/<%= user.image ? user.image : 'default.png' %>" alt="">
            </figure>
            <h3>#<%= user.id %></h3>
        </div>
        <div class="card-content">
            <h3><%= user.first_name %> <%= user.last_name %></h3>
            <p>Categoría: <%= user.category %></p>
            <p>Email: <a href="mailto:<%= user.email %>" target="_blank"><%= user.email %></a></p>
            <div>
               
                <a class="button is-warning" href="/userList/<%= user.id %>/edit">Editar</a>
                <form class="form is-inline" action="/userList/<%= user.id %>?_method=delete" method="POST">
                    <button class="button is-danger">Borrar</button>
                </form>

            </div>
        </div>   
    </article>
</main>

<footer>
<%- include('../partials/footer.ejs') %>
</footer>
</body>
</html>